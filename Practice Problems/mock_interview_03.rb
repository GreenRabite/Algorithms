# write an inplace instance methd on array class that will find the kth
# smallest element in O(n) time. Bonus: How can we elimnate any
# extra  space cost (1st smallest is the 0th element in the sorted array)

class Array
  def quickselect(k)
    
  end
end
